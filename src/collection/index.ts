import Collection from '../game/Collection';

import standard from './standard';
import daybreak from './daybreak';
import bonuspack from './bonus';

const collections: Collection[] = [
	standard,
	daybreak,
	bonuspack,
];

export default collections;
